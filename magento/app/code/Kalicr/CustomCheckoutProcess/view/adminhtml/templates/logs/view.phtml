<?php 
/** @var \Kalicr\CustomCheckoutProcess\Block\Adminhtml\Logs\View $block */ 
?>

<div class="admin__data-grid-wrap" style="margin-bottom: 30px;">
    <h3>Recent Orders (Database)</h3>
    
    <div style="max-height: 600px; overflow-y: auto; border: 1px solid #ccc;">
        <table class="data-grid">
            <thead>
                <tr>
                    <th class="data-grid-th">Invoice / Order ID</th>
                    <th class="data-grid-th">Created At</th>
                </tr>
            </thead>
            <tbody>
                <?php foreach ($block->getRecentOrders() as $order): ?>
                <tr class="data-grid-tr">
                    <td class="data-grid-td">
                        <?= $block->escapeHtml($order->getIncrementId()) ?>
                    </td>
                    <td class="data-grid-td">
                        <?= $block->escapeHtml($order->getCreatedAt()) ?>
                    </td>
                </tr>
                <?php endforeach; ?>
            </tbody>
        </table>
    </div>
</div>

<div class="page-main-actions">
    <div class="page-actions">
        <div class="page-actions-inner">
            <div class="page-actions-buttons">
                <button id="download_log_btn" 
                        title="Download Log" 
                        type="button" 
                        class="action-primary" 
                        onclick="location.href='<?= $block->escapeUrl($block->getDownloadUrl()) ?>'">
                    <span>Download orders.log</span>
                </button>
            </div>
        </div>
    </div>
</div>

<div class="admin__data-grid-wrap" style="padding: 20px; background: #f5f5f5; border: 1px solid #ccc;">
    <h3>Log Content Preview:</h3>
    <div style="background: #fff; padding: 15px; border: 1px solid #ddd; max-height: 600px; overflow-y: auto; white-space: pre-wrap; word-wrap: break-word;">
        <?= $block->escapeHtml($block->getLogContent()) ?>
    </div>
</div>